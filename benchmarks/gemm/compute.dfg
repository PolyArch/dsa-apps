Input: A
Input: BE
Input: BO
Input: C

R = DupHigh32(A)
L = DupLow32(A)

TMP0_REAL = FxMul16x4(L, BE)
TMP0_IMAG = FxMulX16x4(L, BE)
TMP0 = FxSubAdd16x4(TMP0_REAL, TMP0_IMAG)

TMP1_REAL = FxMul16x4(R, BO)
TMP1_IMAG = FxMulX16x4(R, BO)
TMP1 = FxSubAdd16x4(TMP1_REAL, TMP1_IMAG)

SUM = FxAdd16x4(TMP0, TMP1)
O = FxAdd16x4(SUM, C)

Output: O

